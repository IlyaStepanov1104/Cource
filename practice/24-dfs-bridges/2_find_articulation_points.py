"""
Задача: найти все точки сочленения в неориентированном графе.

graph = {0: [1, 2], 1: [0, 2], ...} - список смежности (словарь).
Точка сочленения - вершина, чьё удаление увеличивает число компонент связности.

Переиспользуй код find_bridges из задачи 1 - поменять нужно только условие
и особый случай корня DFS-дерева.
"""


def find_articulation_points(graph: dict) -> set:
    # TODO: реализовать через DFS с tin/low (как в задаче 1)
    # для не-корневой вершины v: точка сочленения, если у неё есть ребёнок to
    # с low[to] >= tin[v]
    # для корня DFS-дерева: точка сочленения, только если у него 2+ детей
    # в самом дереве обхода (условие low >= tin для корня не работает)
    pass


if __name__ == "__main__":
    # два треугольника, соединённых одним мостом (1-3) - пример с занятия
    graph = {
        0: [1, 2],
        1: [0, 2, 3],
        2: [0, 1],
        3: [1, 4, 5],
        4: [3, 5],
        5: [3, 4],
    }
    assert find_articulation_points(graph) == {1, 3}, "точки сочленения - концы моста: 1 и 3"

    # цепочка - обе средние вершины критичны, концы - нет
    chain = {0: [1], 1: [0, 2], 2: [1, 3], 3: [2]}
    assert find_articulation_points(chain) == {1, 2}, "в цепочке критичны все вершины кроме двух концов"

    # простой цикл - точек сочленения нет: убрать любую вершину, остальные всё ещё связаны
    cycle = {0: [1, 3], 1: [0, 2], 2: [1, 3], 3: [2, 0]}
    assert find_articulation_points(cycle) == set(), "в простом цикле точек сочленения нет"

    print("OK: все тесты пройдены")
