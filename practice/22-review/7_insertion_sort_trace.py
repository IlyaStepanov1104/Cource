# Задача: Insertion sort + трассировка
# Реализуй insertion sort и распечатай массив после каждого прохода -
# так проще сверить с тем, что ты расписал на бумаге.
#
# Вход: a - список чисел
# Выход: тот же список, отсортированный по возрастанию


def insertion_sort(a):
    for i in range(1, len(a)):
        key = a[i]
        j = i - 1
        # TODO: сдвигай элементы вправо, пока a[j] > key
        pass
        print(a)  # состояние массива после текущего прохода
    return a


print(insertion_sort([5, 2, 4, 1]))
