# Задача: Пересечение двух массивов за O(n)
# Даны два массива. Найди их пересечение без вложенных циклов -
# используй хеш-таблицу (set).
#
# Вход: a, b - списки int
# Выход: список общих элементов без дублей


def intersection(a, b):
    # TODO: реализуй через set
    pass


print(sorted(intersection([1, 2, 2, 3], [2, 3, 4])))  # [2, 3]
print(sorted(intersection([1, 2], [3, 4])))           # []
