# Задача: Трассировка DFS
# Граф с рёбрами: 1-2, 1-3, 2-4, 3-4.
# Реализуй DFS от вершины 1 и сравни порядок обхода с тем,
# что ты расписал на бумаге.
#
# Вход: graph - словарь {вершина: список соседей}, start - стартовая вершина
# Выход: список вершин в порядке обхода


def dfs(graph, start, visited=None):
    if visited is None:
        visited = []
    # TODO: добавь start в visited, обойди непосещённых соседей рекурсивно
    return visited


graph = {
    1: [2, 3],
    2: [1, 4],
    3: [1, 4],
    4: [2, 3],
}

print(dfs(graph, 1))  # [1, 2, 4, 3]
