# Задача: Лампочки в комнате
# N лампочек. Подсказки вида (i, j, "same") или (i, j, "diff").
# Могут ли все подсказки выполняться одновременно?
# Если да - вернуть состояние каждой лампочки (True=горит), иначе None.
#
# Вход: n, hints - список (i, j, "same"/"diff")
# Выход: список [True/False, ...] или None
#
# Эта задача не требует класса Graph - рёбра имеют тип ("same"/"diff")


def solve_lamps(n, hints):
    pass


# 0==2, 0!=4, 2!=4 - согласовано
print(solve_lamps(5, [(0, 2, "same"), (0, 4, "diff"), (2, 4, "diff")]))

# 0==1, 1==2, 0!=2 - противоречие
print(solve_lamps(3, [(0, 1, "same"), (1, 2, "same"), (0, 2, "diff")]))

# 0!=1, 1!=2, 0!=2 - нечётный цикл, противоречие
print(solve_lamps(3, [(0, 1, "diff"), (1, 2, "diff"), (0, 2, "diff")]))
